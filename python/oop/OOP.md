### OOP

---

```python
class (클래스 이름):
    
    클래스 변수 선언
    
    생성자(constructor)
    def __init__(self,[변수]):
        self.변수 = 변수
        
    소멸자(destructor)
    def __del__(self):
        
    @classmethod
    def (클래스 함수이름)(cls):
        
    @staticmethod   
    def (스태틱 함수이름)():
     
   	def (인스턴스 함수이름)(self)
        
```

